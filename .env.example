# Map-VN uses MapLibre GL + OpenFreeMap by default — no token needed.
# These are all optional for v0.1.

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional — only if you swap MapLibre for Mapbox GL
# NEXT_PUBLIC_MAPBOX_TOKEN=

# Supabase (later, when wiring real data)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

PLAUSIBLE_DOMAIN=
REVALIDATE_SECRET=
